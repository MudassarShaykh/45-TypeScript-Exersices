"use strict";
let PersonNames = ["Abdullah ", "Mudassar ", "Talha "];
let Message = 'Hello ';
let SecondMessage = '"Welcome To Typescript Class."';
for (let k = 0; k < PersonNames.length; k++) {
    console.log(Message + PersonNames[k] + SecondMessage);
}
